spring:
  application:
    name: compost-h
  datasource:
    url: jdbc:mysql://localhost:3306/compost-h
    username: root
    password: root
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
  data:
    rest:
      base-path: /composth
